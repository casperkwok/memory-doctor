"""memory-doctor — keep LLM-agent long-term memory healthy.

v0.1: zero-dependency, zero-LLM. Read-only `report` + deterministic `lint --fix`,
auto-memory format first. See docs/SPEC.md (local, not published).
"""

__version__ = "0.1.0"
